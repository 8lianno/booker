# booker

All instructions live in [AGENTS.md](AGENTS.md). Read it before doing anything in this
repo. Pipeline state is on disk — start every task with `python3 booker.py status <slug>`
(or `list`).
